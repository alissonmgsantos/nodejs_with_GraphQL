type Cliente {
  id: ID!
  nome: String!
  cpf: String!
}

# Sempre que for adicionar, atualizar ou excluir será do tipo Mutation
type Mutation {
  adicionarCliente(nome: String!, cpf: String!): Cliente!
  atualizarCliente(id: ID!, nome: String!, cpf: String!): Cliente!
  deletarCliente(id: ID!): ID!
}

type Query {
  status: String!
  clientes: [Cliente!]!
  cliente(id: ID!): Cliente!
}
